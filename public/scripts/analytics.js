const totalEl = document.getElementById("view-total"); const todayEl = document.getElementById("view-today");
fetch("https://lab2-public-status.wuzhuoyang252.workers.dev/api/views/track", { method: "POST", headers: { "Content-Type": "application/json" }, keepalive: true, body: JSON.stringify({ path: location.pathname }) }).then((res) => res.ok ? res.json() : null).then((data) => { if (!data) return; if (totalEl) totalEl.textContent = Number(data.total || 0).toLocaleString(); if (todayEl) todayEl.textContent = Number(data.today || 0).toLocaleString() + (data.saturated ? "+" : ""); }).catch(() => {});
