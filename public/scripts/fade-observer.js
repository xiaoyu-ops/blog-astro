const revealElements = () => document.querySelectorAll(".fade-up").forEach((el) => el.classList.add("fade-visible"));
if (!("IntersectionObserver" in window)) revealElements(); else { const observer = new IntersectionObserver((entries) => entries.forEach((entry) => { if (entry.isIntersecting) { entry.target.classList.add("fade-visible"); observer.unobserve(entry.target); } }), { threshold: 0.1, rootMargin: "-20px 0px -20px 0px" }); document.querySelectorAll(".fade-up").forEach((el) => observer.observe(el)); }
